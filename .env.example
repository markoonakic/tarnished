# Optional overrides (defaults work for most users)

# External port (default: 5577)
# APP_PORT=5577

# Public URL of your instance (for CORS and TrustedHost)
# APP_URL=https://jobs.yourdomain.com

# Use external PostgreSQL (option 1: full URL)
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/db

# Use external PostgreSQL (option 2: discrete vars - handles special chars)
# POSTGRES_HOST=postgres.example.com
# POSTGRES_PORT=5432
# POSTGRES_USER=tarnished
# POSTGRES_PASSWORD=your-password
# POSTGRES_DB=tarnished

# Override auto-generated secret key
# SECRET_KEY=your-secret-key-here

# PostgreSQL password (required when using docker-compose.postgres.yml)
# POSTGRES_PASSWORD=your-secure-password
