apiVersion: v1
kind: Service
metadata:
  name: {{ include "tarnished.fullname" . }}
  labels:
    {{- include "tarnished.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tarnished.selectorLabels" . | nindent 4 }}
