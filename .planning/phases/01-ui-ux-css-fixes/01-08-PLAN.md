---
phase: 01-ui-ux-css-fixes
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Admin.tsx
  - frontend/src/pages/Applications.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Admin panel has consistent spacing between search input and table"
    - "Table rows have subtle separators following 5-bg layering"
  artifacts:
    - path: "frontend/src/pages/Admin.tsx"
      provides: "Admin panel with proper spacing and table row separators"
      min_lines: 2
    - path: "frontend/src/pages/Applications.tsx"
      provides: "Applications table with row separators"
      min_lines: 1
  key_links:
    - from: "Admin search input"
      to: "Admin table"
      via: "mt-4 or mb-4 spacing class"
      pattern: "mt-4|mb-4"
    - from: "Table rows"
      to: "5-bg layering"
      via: "border-b border-tertiary or similar"
      pattern: "border-b.*border"
---

<objective>
Fix Admin panel spacing and add table row separators

Purpose: User reported the search by email input and table in admin panel have no distance between them. Also, table row separators were removed when container borders were removed - need to add them back following 5-bg layering.

Output: Admin panel has consistent spacing between search input and table; tables have proper row separators
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ux-css-fixes/01-UAT.md
@.planning/phases/01-ui-ux-css-fixes/01-04-SUMMARY.md
@frontend/DESIGN_GUIDELINES.md

# Gap Context
Gap 8 from UAT: "Table Separators Missing"
- User reported: "when removing the borders you also removed the borders which are in the tables seperating the elements (they should only be between elements and maybe also between the top element and header of the table), also the separators i think should also abide by the 5 bg layer rule like the other stuff"
- Root cause: When borders were removed from containers during plan 01-03, table row separators were also removed

Gap 12 from UAT: "All Components Follow 5-Color Layering (Comprehensive Audit)"
- Admin.tsx: Missing gap between search input and table div
- Table rows have no border-bottom or border-top - missing row separators

# Dependency Note
This is Wave 1 - no dependencies on other gap closure plans. This plan can run in parallel with plan 01-05 (input border fixes) as they modify different files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spacing between Admin search input and table</name>
  <files>frontend/src/pages/Admin.tsx</files>
  <action>
    Add spacing class (mt-4 or mb-4) to create consistent gap between search input and table in Admin.tsx.

    Locate:
    - Search input around line 101-107
    - Table container div starting around line 109

    Add mt-4 class to the table container div (line 109):
    Change: <div className="bg-secondary rounded-lg overflow-hidden">
    To: <div className="bg-secondary rounded-lg overflow-hidden mt-4">

    This creates 1rem (16px) spacing consistent with other padding throughout the application.
  </action>
  <verify>grep -A2 "Search by email" frontend/src/pages/Admin.tsx | grep -c "mt-4\|mb-4" returns 1 (spacing class exists between search input and table)</verify>
  <done>Admin panel has consistent 16px spacing between search input and users table</done>
</task>

<task type="auto">
  <name>Task 2: Add table row separators to Admin and Applications tables</name>
  <files>frontend/src/pages/Admin.tsx, frontend/src/pages/Applications.tsx</files>
  <action>
    Add border-b border-tertiary (or context-appropriate border color) to table rows for visual separation following 5-bg layering.

    Admin.tsx:
    Find tbody tr elements (around line 121-122):
    Add border-b border-tertiary class to the tr element:
    Change: <tr key={u.id} className="transition-colors duration-200">
    To: <tr key={u.id} className="transition-colors duration-200 border-b border-tertiary">

    Applications.tsx:
    Find tbody tr elements (around line 161):
    Add border-b border-tertiary class to the tr element:
    Change: <tr key={app.id} className="transition-colors duration-200">
    To: <tr key={app.id} className="transition-colors duration-200 border-b border-tertiary">

    The border-tertiary class uses CSS variable for theme-aware color that follows 5-bg layering.
  </action>
  <verify>grep -n "border-b.*border" frontend/src/pages/Admin.tsx frontend/src/pages/Applications.tsx | grep -i "tr" returns at least 2 (both tables have row borders)</verify>
  <done>Tables in Admin.tsx and Applications.tsx have border-b border-tertiary row separators</done>
</task>

</tasks>

<verification>
1. Run: grep -n "border-b" frontend/src/pages/Admin.tsx frontend/src/pages/Applications.tsx | grep "tr\|thead" to verify table rows have separators

2. Run: npm run build to ensure no TypeScript errors

3. Manual check:
   - Visit Admin page, verify spacing between search input and table
   - Verify table rows have subtle separator lines
   - Visit Applications page, verify table rows have separator lines
</verification>

<success_criteria>
1. Admin panel has 16px spacing between search input and table
2. Admin table rows have border-b border-tertiary separators
3. Applications table rows have border-b border-tertiary separators
4. Table separators are subtle and theme-aware
5. Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-ux-css-fixes/01-08-SUMMARY.md`
</output>
