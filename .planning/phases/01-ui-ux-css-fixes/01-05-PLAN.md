---
phase: 01-ui-ux-css-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/RoundForm.tsx
  - frontend/src/components/CreateUserModal.tsx
  - frontend/src/components/PasswordInput.tsx
  - frontend/src/components/EditUserModal.tsx
  - frontend/src/components/ImportModal.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/components/ActivityHeatmap.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All input elements display aqua-bright border on focus with smooth transition"
    - "Input focus border is clearly visible against the input background"
  artifacts:
    - path: "frontend/src/components/RoundForm.tsx"
      provides: "Input elements with border class"
      min_lines: 12
    - path: "frontend/src/components/CreateUserModal.tsx"
      provides: "Input elements with border class"
      min_lines: 2
    - path: "frontend/src/components/PasswordInput.tsx"
      provides: "Password input with border class"
      min_lines: 1
    - path: "frontend/src/components/EditUserModal.tsx"
      provides: "Input elements with border class"
      min_lines: 1
    - path: "frontend/src/components/ImportModal.tsx"
      provides: "File input with border class"
      min_lines: 1
    - path: "frontend/src/pages/Settings.tsx"
      provides: "Select inputs with border class"
      min_lines: 4
    - path: "frontend/src/components/ActivityHeatmap.tsx"
      provides: "Select input with border class"
      min_lines: 1
  key_links:
    - from: "All input elements"
      to: "CSS focus:border-aqua-bright"
      via: "base border class (border border-tertiary)"
      pattern: "border border-tertiary.*focus:border-aqua-bright"
---

<objective>
Add base border class to all input elements so focus:border-aqua-bright works correctly

Purpose: Input focus states (aqua-bright border) are not visible because inputs lack the base 'border' class. By default, HTML elements have border-style: none, so focus:border-* modifiers have no visual effect without a base border class.

Output: All input/textarea/select elements have border border-tertiary (or context-appropriate border color) followed by focus:border-aqua-bright
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ux-css-fixes/01-UAT.md
@.planning/phases/01-ui-ux-css-fixes/01-00-SUMMARY.md
@.planning/phases/01-ui-ux-css-fixes/01-04-SUMMARY.md
@frontend/DESIGN_GUIDELINES.md

# Gap Context
Gap 1 from UAT: "Input Focus Border Missing Base Border Class"
- User reported: "none of the inputs actually have this currently (its broken or whatever its not working)"
- Root cause: Input elements missing base 'border' class
- The CSS class .focus\:border-aqua-bright:focus IS being generated correctly by Tailwind v4, but inputs have no border to color

# Dependency Note
This is Wave 1 - no dependencies on other gap closure plans. This plan can run in parallel with plan 01-08 (Admin/table fixes) as they modify different files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add border class to form inputs in RoundForm.tsx</name>
  <files>frontend/src/components/RoundForm.tsx</files>
  <action>
    Add 'border border-tertiary' to all input, select, and textarea elements that use focus:border-aqua-bright but lack the base border class.

    Locations (lines with focus:border-aqua-bright but no border class):
    - Line 158: select (round type dropdown)
    - Line 175: input (scheduled date)
    - Line 186: input (scheduled time)
    - Line 197: select (outcome)
    - Line 212: input (completed date)
    - Line 223: input (completed time)
    - Line 236: textarea (notes)
    - Line 246: input (transcript file)
    - Line 268: textarea (transcript summary)

    Pattern to apply:
    Change: className="w-full px-3 py-2 bg-bg3 text-fg1 focus:border-aqua-bright focus:outline-none transition-all duration-200 ease-in-out rounded"
    To: className="w-full px-3 py-2 bg-bg3 text-fg1 border border-tertiary focus:border-aqua-bright focus:outline-none transition-all duration-200 ease-in-out rounded"

    Reference existing pattern from DESIGN_GUIDELINES.md line 162-166 for standard input pattern.
  </action>
  <verify>grep -n "border.*focus:border-aqua-bright" frontend/src/components/RoundForm.tsx | wc -l returns 9 (all inputs now have border class)</verify>
  <done>All 9 input elements in RoundForm.tsx have border border-tertiary class before focus:border-aqua-bright</done>
</task>

<task type="auto">
  <name>Task 2: Add border class to inputs in modal components</name>
  <files>frontend/src/components/CreateUserModal.tsx, frontend/src/components/EditUserModal.tsx, frontend/src/components/ImportModal.tsx, frontend/src/components/PasswordInput.tsx</files>
  <action>
    Add 'border border-tertiary' to all input elements in the following files:

    CreateUserModal.tsx (2 inputs):
    - Line 89: email input
    - Line 103: password input

    EditUserModal.tsx (1 input):
    - Line 155: new password input

    ImportModal.tsx (1 input):
    - Line 205: file input

    PasswordInput.tsx (1 input):
    - Line 26: password input

    Apply same pattern as Task 1: add "border border-tertiary" before "focus:border-aqua-bright"
  </action>
  <verify>
    grep -n "border.*focus:border-aqua-bright" frontend/src/components/CreateUserModal.tsx frontend/src/components/EditUserModal.tsx frontend/src/components/ImportModal.tsx frontend/src/components/PasswordInput.tsx | wc -l returns 5
  </verify>
  <done>All 5 input elements across modal components have border border-tertiary class before focus:border-aqua-bright</done>
</task>

<task type="auto">
  <name>Task 3: Add border class to Settings and ActivityHeatmap inputs</name>
  <files>frontend/src/pages/Settings.tsx, frontend/src/components/ActivityHeatmap.tsx</files>
  <action>
    Add 'border border-tertiary' to all select/input elements in Settings.tsx and ActivityHeatmap.tsx:

    Settings.tsx (4 inputs):
    - Line 277: mobile dropdown select (activeSection selector)
    - Any other input/select elements using focus:border-aqua-bright

    ActivityHeatmap.tsx (1 input):
    - Find the view mode select/input element using focus:border-aqua-bright

    Apply same pattern: add "border border-tertiary" before "focus:border-aqua-bright"
  </action>
  <verify>grep -n "border.*focus:border-aqua-bright" frontend/src/pages/Settings.tsx frontend/src/components/ActivityHeatmap.tsx | wc -l returns at least 5</verify>
  <done>All select/input elements in Settings.tsx and ActivityHeatmap.tsx have border border-tertiary class before focus:border-aqua-bright</done>
</task>

</tasks>

<verification>
1. Run: grep -rn "focus:border-aqua-bright" frontend/src/components frontend/src/pages --include="*.tsx" | grep -v "border " | wc -l returns 0 (no inputs with focus:border-aqua-bright but no base border)

2. Run: npm run build to ensure no TypeScript errors

3. Manual check: Click into any text input field and verify the border glows with aqua-bright color
</verification>

<success_criteria>
1. All input/textarea/select elements have 'border border-tertiary' class
2. Focus states display aqua-bright border color on all inputs
3. Build passes without errors
4. No regression in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-ux-css-fixes/01-05-SUMMARY.md`
</output>
