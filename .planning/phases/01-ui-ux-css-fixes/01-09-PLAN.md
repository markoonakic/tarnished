---
phase: 01-ui-ux-css-fixes
plan: 09
type: execute
wave: 4
depends_on: ["07"]
files_modified:
  - frontend/src/components/application/StatusHistoryModal.tsx
  - frontend/src/components/ImportModal.tsx
  - frontend/src/components/CreateUserModal.tsx
  - frontend/src/components/EditUserModal.tsx
  - frontend/src/components/MediaPlayer.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "X icon-only close buttons have appropriately sized hover background (not oversized)"
    - "Close button hover area is proportional to icon size"
  artifacts:
    - path: "frontend/src/components/application/StatusHistoryModal.tsx"
      provides: "Close button with reduced padding"
      min_lines: 1
    - path: "frontend/src/components/ImportModal.tsx"
      provides: "Close button with reduced padding"
      min_lines: 1
    - path: "frontend/src/components/CreateUserModal.tsx"
      provides: "Close button with reduced padding"
      min_lines: 1
    - path: "frontend/src/components/EditUserModal.tsx"
      provides: "Close button with reduced padding"
      min_lines: 1
    - path: "frontend/src/components/MediaPlayer.tsx"
      provides: "Close button with reduced padding"
      min_lines: 1
  key_links:
    - from: "Close button"
      to: "Appropriately sized hover area"
      via: "px-2 py-1 or px-2 py-1.5 with text-lg icon"
      pattern: "px-2.*text-lg\|px-1.*text-lg"
---

<objective>
Fix X icon button padding to create appropriately sized hover background

Purpose: User reported "the bg on hover of the X icon only buttons are too big (the bg is way to big for the icon itself)". The X icon close buttons in all modals use px-3 py-1.5 padding with text-xl icon, creating an oversized hover area.

Output: All X icon close buttons use reduced padding (px-2 py-1 or px-2 py-1.5) with text-lg icon for properly proportioned hover area
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-ux-css-fixes/01-UAT.md
@.planning/phases/01-ui-ux-css-fixes/01-04-SUMMARY.md
@frontend/DESIGN_GUIDELINES.md

# Gap Context
Gap 2 from UAT: "Icon-Only Close Buttons Have Oversized Hover Background"
- User reported: "the bg on hover of the X icon only buttons are too big (the bg is way to big for the icon itself)"
- Root cause: X icon close buttons use px-3 py-1.5 padding with text-xl icon
- The asymmetric padding was meant for taller icons, but text-xl icon size creates oversized hover area

Locations:
- StatusHistoryModal.tsx line 47
- ImportModal.tsx line 129
- CreateUserModal.tsx line 68
- EditUserModal.tsx line 96
- MediaPlayer.tsx line 98

# Dependency Note
This is Wave 4 - depends on plan 01-07 (button defaults). Must run after button fixes are complete as this plan modifies the same modal close button elements in modal files that were changed in earlier plans.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix X icon button padding in all modal close buttons</name>
  <files>frontend/src/components/application/StatusHistoryModal.tsx, frontend/src/components/ImportModal.tsx, frontend/src/components/CreateUserModal.tsx, frontend/src/components/EditUserModal.tsx, frontend/src/components/MediaPlayer.tsx</files>
  <action>
    Reduce close button padding and/or icon size to create appropriately sized hover background.

    For each modal file, find the close button with bi-x-lg text-xl icon and px-3 py-1.5 padding, then apply one of these fixes:

    Option A (preferred): Reduce padding to px-2 py-1 (smaller, more proportional):
    Change: className="text-fg1 hover:bg-bg2 hover:text-fg0 px-3 py-1.5 rounded transition-all duration-200 ease-in-out cursor-pointer"
    To: className="text-fg1 hover:bg-bg2 hover:text-fg0 px-2 py-1 rounded transition-all duration-200 ease-in-out cursor-pointer"

    Option B: Reduce icon size from text-xl to text-lg (keeps padding, smaller icon):
    Change: <i className="bi bi-x-lg text-xl" />
    To: <i className="bi bi-x-lg text-lg" />

    Files to update:
    1. StatusHistoryModal.tsx line 46-47: close button
    2. ImportModal.tsx line 127-129: close button
    3. CreateUserModal.tsx line 66-68: close button
    4. EditUserModal.tsx line 94-96: close button
    5. MediaPlayer.tsx line 94-98: close button

    Apply Option A (reduce padding to px-2 py-1) to all 5 modal close buttons for consistency.
  </action>
  <verify>grep -n "px-3 py-1.5.*bi-x-lg\|px-2 py-1.*bi-x-lg" frontend/src/components/application/StatusHistoryModal.tsx frontend/src/components/ImportModal.tsx frontend/src/components/CreateUserModal.tsx frontend/src/components/EditUserModal.tsx frontend/src/components/MediaPlayer.tsx | grep -c "px-2 py-1" returns 5 (all close buttons use reduced padding)</verify>
  <done>All 5 modal close buttons use px-2 py-1 padding for appropriately sized hover background</done>
</task>

</tasks>

<verification>
1. Run: grep -rn "px-3 py-1.5.*bi-x-lg" frontend/src/components/application/StatusHistoryModal.tsx frontend/src/components/ImportModal.tsx frontend/src/components/CreateUserModal.tsx frontend/src/components/EditUserModal.tsx frontend/src/components/MediaPlayer.tsx returns no results (no close buttons with oversized padding)

2. Run: npm run build to ensure no TypeScript errors

3. Manual check: Open any modal and hover over the X close button - the hover background should be proportionate to the icon size
</verification>

<success_criteria>
1. All modal close buttons use px-2 py-1 padding (not px-3 py-1.5)
2. Close button hover area is appropriately sized (not oversized)
3. Close buttons remain easy to click (not too small)
4. Build passes without errors
5. No regression in modal close functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-ux-css-fixes/01-09-SUMMARY.md`
</output>
